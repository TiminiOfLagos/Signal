{
  "name": "signal-sessions",
  "version": "0.3.0",
  "private": true,
  "description": "SIGNAL — Sessions 03. Campaign site for a studio residency for artists, producers and engineers across West and East Africa.",
  "//": "No build step and no server in production — this is a static site. server.js is for local development only, which is why there is no 'main' or 'build' field for a host to auto-detect.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "license": "UNLICENSED"
}
